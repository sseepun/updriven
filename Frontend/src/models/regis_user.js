export default class RegisUser {
    constructor(
        password, 
        email, 
        firstname,
        lastname) {
      this.password = password;
      this.email = email;
      this.firstname = firstname;
      this.lastname = lastname;
    }
  }